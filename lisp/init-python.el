;; -*- lexical-binding: t; -*-

;; (add-hook 'python-mode-hook
;; 	  #'(lambda ()
;; 	      (require 'lsp-pyright)
;; 	      (lsp)))

;; (with-eval-after-load 'lsp-pyright
;;   (lsp))

(provide 'init-python)
